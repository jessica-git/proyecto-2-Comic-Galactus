<H1>DETALLES DE TU CÓMIC</H1>

<div class="row">
  {{#each comicDB}}
  <div class="col-md-4">
    <div class="card">
      <img class="card-img-top" src="{{this.image}}" alt="{{this.name}}">
      <div class="card-body">
        <h5 class="card-title">{{this.name}}</h5>
        <p class="card-text">{{{this.description}}}</p>
        <p class="card-text">{{this.issuesNumber}}</p>
        <p class="card-text">{{this.resourceTypes}}</p>
        <p class="card-text">{{this.volumen}}</p>
        <p class="card-text">{{this.publisherName}}</p>
      </div>
    </div>
  </div>
  {{/each}}
</div>